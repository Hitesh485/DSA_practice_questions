#include <iostream>

// if height of left subtree & right subtree -> abs(lt - rt) <= 1
// either -1, 0, 1.

using namespace std;

class Node
{
    public:
    int data;
    Node* left = NULL;
    Node* right = NULL;
};

Node* buildTree()

int main ()
{
    
    return 0;
}